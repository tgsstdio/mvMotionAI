mvMotionAI developer (Source/Headers Only Version) 
for version 00-01-21

The code included is written in C/C++ with Lua 5.0 API. Lua Script files 
have been included.

The test application (GLMain.cpp) needs OpenGL, GLUT, Lua 5.0 and the script file Test.lua to compile and run.

All source & headers files provided are under the MIT license.

Compile Instrution: compile all source files into a static library and then with the appropriate Lua 5.0 library objects (i.e DLL + libs or .a object files) along with your application.

This release contains no executables or library objects.

David Young,
5 Dec 2006


